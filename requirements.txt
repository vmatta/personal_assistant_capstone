# Core Frameworks & Graph Orchestration
# Pinned to exact versions verified to install cleanly together on Python 3.11.9.
# Do not loosen these to >= without re-testing a full `pip install -r requirements.txt`
# from a clean venv -- crewai's dependency tree is large and version drift breaks easily.
langgraph==1.2.11
langchain-core==1.6.2
langchain-openai==1.6.0
crewai==1.15.20

# Vector Database & Embeddings
chromadb==1.1.1
sentence-transformers==6.0.1

# Configuration, Serialization & Environment
pydantic==2.12.5
pyyaml==6.0.3
python-dotenv==1.2.3
streamlit==1.63.0
packaging==26.3

# HTTP & MCP Protocol Client Utilities
requests==2.34.2
httpx==0.28.1
tenacity==9.1.4

# Testing
pytest==9.1.1